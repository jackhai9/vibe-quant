# Input: 运行时环境变量
# Output: systemd EnvironmentFile 模板
# Pos: env 示例文件
# 一旦我被更新，务必更新我的开头注释，以及所属文件夹的MD。

# /etc/binance-exit-executor/binance-exit-executor.env
#
# 说明：
# - 该文件应由 systemd 的 EnvironmentFile= 引用
# - 请勿提交包含真实密钥的文件到 git

# Binance API
BINANCE_API_KEY=replace_me
BINANCE_API_SECRET=replace_me

# Telegram（可选；仅当 config.yaml 中 global.telegram.enabled=true 时需要）
TELEGRAM_BOT_TOKEN=replace_me
TELEGRAM_CHAT_ID=replace_me

# 代理配置说明：
# - 不要在此设置 HTTP_PROXY / HTTPS_PROXY 环境变量（系统不会读取）
# - 代理应在 config.yaml 的 global.proxy 中配置

# 日志目录（可选；systemd 模板已设置为 /var/log/binance-exit-executor）
# VQ_LOG_DIR=/var/log/binance-exit-executor

