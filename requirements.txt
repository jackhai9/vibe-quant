# Input: 依赖版本清单
# Output: pip requirements
# Pos: 依赖清单
# 一旦我被更新，务必更新我的开头注释，以及所属文件夹的MD。

# vibe-quant 依赖

# 交易所相关
ccxt>=4.0.0
aiohttp>=3.9.0

# WebSocket
websockets>=12.0

# 配置
PyYAML>=6.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# 日志
loguru>=0.7.0

# 通知（Telegram Bot API：使用 aiohttp 直连，无额外依赖）

# 开发依赖
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
pyright>=1.1.0
